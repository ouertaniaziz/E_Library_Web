<form method="post" action="{{ path('app_users_delete', {'idUser': user.idUser}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet utilisateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.idUser) }}">
    <button class="btn">Delete</button>
</form>
