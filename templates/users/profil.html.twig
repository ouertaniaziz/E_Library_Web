{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    <center>
        {{ form_widget(form.nomUser,{"attr":{"placeholder":"Nom"}})}}
        <font color="red">
            {{ form_errors(form.nomUser)}}
        </font >
        <br>
        <br>
        {{ form_widget(form.prenomUser,{"attr":{"placeholder":"Prenom"}})}}
        <font color="red">
            {{ form_errors(form.prenomUser)}}
        </font >
        <br>
        <br>
        {{ form_widget(form.emailUser,{"attr":{"placeholder":"Email"}})}}
        <font color="red">
            {{ form_errors(form.emailUser)}}
        </font >
        <br>
        <br>

        {{ form_widget(form.telUser,{"attr":{"placeholder":"Téléphone"}})}}
        <font color="red">
            {{ form_errors(form.telUser)}}
        </font >
        <br>
        <br>
        {{ form_widget(form.adresse,{"attr":{"placeholder":"Adresse"}})}}
        <font color="red">
            {{ form_errors(form.adresse)}}
        </font >
        <br>
        <br>
        <button type="submit" class="btn btn-outline-primary btn-fw">{{ button_label|default('Modifier') }}</button>
        <br>
        <a href="{{ path('app_login_newMdp') }}">Modifier le mot de passe</a>
    </center>
    {{ form_end(form) }}
    <a href="{{ path('app_home', {'idUser': app.session.get('idUser')}) }}">back to list</a>


{% endblock %}