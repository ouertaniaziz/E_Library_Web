<form method="post" action="{{ path('app_emprunt_delete', {'idEmprunt': emprunt.idEmprunt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emprunt.idEmprunt) }}">
    <button class="btn">Delete</button>
</form>
