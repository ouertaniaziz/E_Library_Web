{% extends 'base.html.twig' %}

{% block title %}Edit Auteur{% endblock %}

{% block body %}
    <h1>Edit Auteur</h1>
 {#   <form method="POST" action="{{ path('upload_test') }}" enctype="multipart/form-data">
    <input type="file" name="image">
    <button type="submit" class="btn btn-check">envoi</button>
    </form>
 #}
    {{ include('auteur/_form.html.twig', {'button_label': 'Update'}) }}

    <button class="btn btn-primary"  href="{{ path('app_auteur_index') }}">back to list</button>

    {{ include('auteur/_delete_form.html.twig') }}
{% endblock %}
